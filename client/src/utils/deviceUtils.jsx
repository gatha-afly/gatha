/**
 * Checks if the window width is less than or equal to 768 pixels to determine if it's a mobile device.
 * @returns {boolean} True if the window width is less than or equal to 768 pixels, indicating a mobile device.
 */
export const isMobile = window.innerWidth <= 768;
